#include "stdafx.h"

extern std::wstring MTW(const char* string);

extern float soundVolume;
extern float musicVolume;

extern int killEnemyNum;//죽인 몬스터 수(전체 몬스터 수(5*6)으로 나눈 값 표시)
extern int getCouponNum;//얻은 쿠폰수(총 50개)
extern int useRebuildNum;//리빌드 사용회수
extern int useHintNum;//힌트 사용회수

extern bool cheatButton;//버튼치트(리빌드무한)
extern bool cheatMap;//맵핵

extern void resetChallenge();
